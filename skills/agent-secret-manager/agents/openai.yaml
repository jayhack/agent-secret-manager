interface:
  display_name: "Agent Secret Manager"
  short_description: "Manage local secrets without exposing values"
  default_prompt: "Use $agent-secret-manager to request missing secrets with npx agent-secret-manager, verify only presence, and never print or read secret values."
