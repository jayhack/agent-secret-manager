interface:
  display_name: "Agent Secrets"
  short_description: "Request local secrets without exposing values"
  default_prompt: "Use $agent-secrets to request missing API keys without printing or reading secret values."
